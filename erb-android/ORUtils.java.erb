// DO NOT MODIFY!
// Generated by mb-ormapper generator ver <%= VER %>.
// https://github.com/tmurakam/mb-ormapper

package <%= PKGNAME %>;

//import org.tmurakam.ormapper.ORDatabase;

import java.io.Writer;
import java.io.IOException;

public class ORUtils {
    private final static String TAG = "ORUtils";
    
    public static void migrate() {
<% $class_defs.each do |cdef| -%>
        <%= cdef.class_name %>.migrate();
<% end -%>
    }

    public static void getDbSql(Write wr) throws IOException {
        //int schema_version = ORDatabase.getDB().getVersion();
        //wr.write("PRAGMA schema_version = " + schema_version + ";");

<% $class_defs.each do |cdef| -%>
        <%= cdef.class_name %>.getTableSql(wr);
<% end -%>
    }
}

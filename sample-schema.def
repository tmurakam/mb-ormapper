# output dir (Android only)
OUTDIR = src

# Package name (Android only)
PKGNAME = com.example.person

# Primary key name (iOS only)
#PKEY = key

# class definition
person: Person, PersonBase
	name: TEXT
     	sex: INTEGER
        age: INTEGER
        birth_date: DATE
        phone_number: TEXT

group: Group
       name: TEXT
       people: has_many Person, group_id
